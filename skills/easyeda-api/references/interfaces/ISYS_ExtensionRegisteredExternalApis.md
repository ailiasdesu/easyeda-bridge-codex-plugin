# ISYS\_ExtensionRegisteredExternalApis interface

扩展注入的上层接口注册表（按扩展分组）

## Signature

```typescript
interface ISYS_ExtensionRegisteredExternalApis
```

## Remarks

键结构：extensionUuid -<!-- -->&gt; namespace -<!-- -->&gt; apiName -<!-- -->&gt; 注册条目 便于按扩展卸载时整组清理