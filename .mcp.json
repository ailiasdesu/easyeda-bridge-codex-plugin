{
  "_comment": "克隆后请先运行 scripts\\install.ps1：它会把 command/args 改写为本机绝对路径（Codex 启动 MCP 时不会展开相对路径）。",
  "mcpServers": {
    "easyeda_bridge": {
      "command": "node",
      "args": [
        "./scripts/easyeda-bridge-mcp.mjs"
      ]
    }
  }
}
